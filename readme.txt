Type on the command line: node weather.js

Then type on the browsers: http://localhost:12345/weather59ddbeed4778dd00?name=foo

59ddbeed4778dd00 is the API id of the user.

[?name=foo] is optional. ? is the split symbol, name=foo is the parameter to log.


